#include <stdio.h>
int main()
{
    int a,b,sum1,sum2;
    a=10;
    b=20;
    printf("Before Swapping");
    printf("\na=%d",a);
    printf("\nb=%d",b);
    printf("\nAfter Swapping");
    sum1=0+a==b;
    printf("\na=%d",b);
    sum2=0+b==a;
    printf("\nb=%d",a);
    return 0;
}
